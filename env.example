# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/tbt-companion

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/tbt-companion?retryWrites=true&w=majority

# Firebase Admin SDK Configuration
# Get these values from your Firebase service account JSON file
# (Firebase Console → Project Settings → Service Accounts → Generate New Private Key)

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# CORS Configuration
# Set to your frontend URL in production
# For development with iOS simulator, * is acceptable
CORS_ORIGIN=*

# Optional: Logging
# LOG_LEVEL=debug

